# MASTER PROMPT — HARDEN AND UPGRADE MY EXISTING FACE VERIFICATION SYSTEM

You are a senior biometric security architect, face recognition engineer, anti-spoofing specialist, and authentication security engineer.

I already have a working website, frontend, backend, database, authentication system, and face verification service.

Do NOT rebuild the application.

Do NOT generate a new architecture from scratch.

Your task is to audit, identify weaknesses, and upgrade the existing biometric authentication pipeline so that unauthorized users, unenrolled users, lookalikes, photos, replay attacks, and low-confidence matches are rejected.

Treat the existing implementation as production software that must be hardened.

The primary goal is to reduce False Acceptance Rate (FAR) while maintaining acceptable user experience.

---

# FEATURE PRESERVATION REQUIREMENTS

The existing application is already operational.

Under no circumstances may existing functionality be removed, disabled, downgraded, bypassed, or replaced unless a security vulnerability, correctness issue, or explicit incompatibility is identified.

Before modifying any component:

1. Analyze existing functionality.
2. Determine all current behaviors.
3. Preserve all existing business logic.
4. Preserve all existing API contracts unless required for security.
5. Preserve all existing user flows.
6. Preserve all existing authentication flows.
7. Preserve all existing integrations.

Any modification must be implemented as:

* additive enhancement
* security hardening
* performance improvement
* bug fix

Never perform destructive refactoring.

Never remove functionality solely because a different implementation exists.

Never remove features to simplify implementation.

If a component must be replaced:

* document the reason
* document the security benefit
* preserve backward compatibility
* provide migration logic

The upgraded system must retain all current capabilities while adding security and verification improvements.

---

# PIPELINE INTEGRITY ANALYSIS AND AUTO-REPAIR REQUIREMENTS

The AI must perform a complete end-to-end pipeline audit before making modifications.

Analyze:

* frontend flow
* backend flow
* authentication flow
* enrollment flow
* verification flow
* liveness flow
* storage flow
* session flow
* logging flow
* monitoring flow

Build a complete dependency map.

Identify:

* broken links
* missing validations
* race conditions
* dead code
* insecure code paths
* bypass opportunities
* weak thresholds
* missing exception handling
* pipeline inconsistencies
* integration failures
* state management errors
* database inconsistencies
* API contract violations

For every identified issue:

1. determine root cause
2. determine impact
3. generate correction
4. validate correction against entire pipeline

No modification may introduce new pipeline failures.

Every modified component must be validated against upstream and downstream dependencies.

Perform automatic consistency validation after every major modification.

The final system must pass full pipeline integrity verification.

---

# CONTINUOUS CHECKPOINT AND RESUME REQUIREMENTS

The implementation may be interrupted because of:

* token limits
* credit exhaustion
* context window limits
* response truncation
* session interruption

The AI must maintain resumable checkpoints.

Before beginning work:

Generate:

PROJECT STATE REPORT

Including:

* project modules discovered
* components analyzed
* pending components
* completed components

For every completed section generate:

CHECKPOINT ID

Include:

* component name
* files modified
* status
* dependencies
* migration status
* validation status

Checkpoint format:

[CHECKPOINT]
ID: CP-001
MODULE: Face Enrollment
STATUS: Completed
FILES: [...]
DEPENDENCIES: [...]
VALIDATION: Passed
[/CHECKPOINT]

At the end of every response generate:

CURRENT COMPLETION STATUS

Including:

* percentage completed
* completed checkpoints
* next checkpoint
* remaining work

If interrupted:

The next session must be able to continue from the latest checkpoint without redoing completed work.

The AI must always identify the most recent checkpoint and resume from that point.

Never restart the entire implementation when checkpoint data exists.

---

# REGRESSION PREVENTION REQUIREMENTS

Before finalizing any modification:

Perform regression analysis.

Verify:

* registration still works
* login still works
* face enrollment still works
* face verification still works
* session creation still works
* JWT still works
* refresh tokens still work
* database migrations remain valid
* APIs remain functional
* existing users remain functional

Generate regression report.

Any modification that introduces a regression must be rejected and replaced with a compatible implementation.

---

# IMPLEMENTATION SAFETY REQUIREMENTS

Security improvements must never come at the cost of application stability.

Priority order:

1. Security
2. Correctness
3. Stability
4. Performance
5. Maintainability

All generated code must satisfy all five priorities.

No change may reduce security, correctness, or stability in order to improve performance.

---


---

# CURRENT PROBLEM

The current face verification system:

* accepts some unenrolled users
* accepts incorrect users
* produces false positives
* is not strict enough
* is not secure enough

Your task is to find every weakness and replace weak components with production-grade implementations.

---

# TARGET SECURITY OBJECTIVE

Implement the closest practical web equivalent of Windows Hello and Face ID using only standard webcams.

Assume:

* no infrared camera
* no depth sensor
* no TPM access
* no Secure Enclave access
* no biometric hardware APIs

The solution must compensate through software controls.

---

# REQUIRED AUDIT PROCESS

Before generating modifications:

1. Audit current enrollment process
2. Audit current embedding generation process
3. Audit current matching process
4. Audit current liveness process
5. Audit current threshold configuration
6. Audit current session creation process
7. Audit current authentication pipeline

Identify every weakness.

Replace weak implementations automatically.

---

# FACE ENROLLMENT UPGRADE REQUIREMENTS

Replace any single-image enrollment.

Require:

* front face
* left turn
* right turn
* slight up
* slight down
* neutral face

Generate multiple embeddings.

Store 5–10 embeddings minimum.

Create biometric profile templates.

Reject enrollment quality below configured thresholds.

Implement enrollment quality scoring.

Reject:

* blurry faces
* occluded faces
* low-light faces
* partially visible faces

---

# FACE QUALITY ASSESSMENT

Before generating embeddings:

Validate:

* brightness
* sharpness
* face size
* face visibility
* pose quality

Reject poor-quality samples.

No embedding may be stored unless quality checks pass.

---

# FACE ALIGNMENT UPGRADE

Before embedding generation:

* detect landmarks
* align face
* normalize rotation
* normalize scale
* normalize orientation

Embeddings generated from unaligned faces must be prohibited.

---

# EMBEDDING UPGRADE

Use current state-of-the-art InsightFace ArcFace embeddings.

Require:

* 512-dimensional vectors
* normalized embeddings

Support embedding versioning.

Support future model upgrades.

Store model version for every embedding.

---

# MATCHING ENGINE HARDENING

Replace any single-comparison strategy.

For login:

Compare against every enrollment embedding.

Calculate:

* maximum similarity
* average similarity
* median similarity
* confidence score

Authentication must require:

* minimum confidence threshold
* minimum similarity threshold
* minimum enrollment agreement threshold

Reject otherwise.

---

# FALSE ACCEPTANCE REDUCTION

Prioritize security over convenience.

Reduce FAR aggressively.

Implement:

* threshold calibration
* confidence scoring
* similarity distribution analysis

Prevent acceptance based on a single high score.

Require consistency across enrolled embeddings.

---

# LIVENESS HARDENING

Reject:

* photos
* screenshots
* mobile displays
* replay videos
* deepfake attempts

Implement:

1. blink verification
2. head turn verification
3. challenge-response verification
4. anti-spoofing neural network

Randomize challenges.

Authentication must fail if any challenge fails.

---

# DEVICE TRUST LAYER

Implement device reputation.

Track:

* browser fingerprint
* device fingerprint
* operating system
* user agent consistency

Known trusted devices receive lower risk.

Unknown devices increase verification strictness.

---

# RISK ENGINE

Calculate authentication risk score.

Inputs:

* face confidence
* liveness confidence
* device trust
* login history
* IP history
* geographic consistency
* velocity anomalies

High-risk logins require additional verification.

---

# PASSKEY INTEGRATION

Implement WebAuthn support.

Face verification should become one factor.

Support:

* passkeys
* platform authenticators
* discoverable credentials
* user verification

Combine biometric verification with modern authentication standards.

---

# BIOMETRIC STORAGE HARDENING

Do not store plaintext embeddings.

Require:

AES-256-GCM encryption

Support:

* key rotation
* key versioning
* biometric template versioning

---

# SESSION SECURITY HARDENING

Face verification alone must not create permanent trust.

Require:

* JWT expiration
* refresh token rotation
* session revocation
* device-bound sessions

---

# RATE LIMITING HARDENING

Face login failures:

5 failures → 5 min lock

10 failures → 30 min lock

15 failures → 24 hour lock

Store lock state in Redis.

---

# LOGGING AND FORENSICS

Log:

* similarity scores
* confidence scores
* liveness results
* spoof detection results
* device information
* authentication decisions

Provide complete audit trail.

---

# PERFORMANCE TARGETS

Verification:
< 500ms

Authentication:
< 2 seconds

Maintain security while meeting performance targets.

---

# OUTPUT REQUIREMENTS

Do not redesign the entire application.

Analyze existing codebase.

Identify weaknesses.

Generate exact modifications.

Generate replacement code only where improvements are required.

Explain why each modification reduces false acceptance and improves security.

Focus on improving verification accuracy and authentication security rather than generating unrelated infrastructure.
